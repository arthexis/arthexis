[project]
name = "arthexis"
aliases = ["constellation"]

[adapter]
type = "django"
manage = "manage.py"
settings = "config.settings"

# GWAY owns the managed checkout and Python environment described here.
# Arthexis lifecycle hooks only perform application-specific preparation.
[install]
root = "/opt/arthexis"
checkout = "app"
environment = ".venv"

[lifecycle]
install = "apps.core.system.lifecycle:install"
upgrade = "apps.core.system.lifecycle:upgrade"
