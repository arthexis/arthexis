name: Ruff Autofix

on:
  pull_request:

jobs:
  autofix:
    name: Ruff autofix
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Check out pull request head
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
          persist-credentials: true
      - uses: actions/setup-python@v7
        with:
          python-version: '3.14'
          cache: pip
      - name: Install normalization tools
        run: python -m pip install --upgrade pip ruff pyupgrade
      - name: Apply Python modernization fixes
        run: |
          find apps arthexis config scripts utils tests -type f -name '*.py' -print0 \
            | xargs -0 -r pyupgrade --py311-plus || true
          python -m ruff check . --fix --unsafe-fixes --exit-zero
          python -m ruff format .
      - name: Commit fixes
        env:
          HEAD_REF: ${{ github.event.pull_request.head.ref }}
        run: |
          if git diff --quiet; then
            echo "No normalization changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add --all
          git commit -m "style: apply Python normalization fixes"
          git push origin "HEAD:$HEAD_REF"
