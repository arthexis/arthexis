{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Update requirements",
            "type": "shell",
            "command": "${config:python.defaultInterpreterPath} -m pip install -U -r requirements.txt && ${config:python.defaultInterpreterPath} -m pip freeze > requirements.txt",
            "problemMatcher": []
        }
    ]
}
