[
  {
    "model": "core.todo",
    "fields": {
      "request": "Manual QA: Confirm auto-upgrade restarts services when sudo requires a password",
      "created_on": "2025-11-15T00:00:00Z",
      "url": "/admin/system/upgrade-report/",
      "request_details": "1. On a managed node (locks/service.lck present), run `sudo -K` and confirm `sudo -n true` exits with a non-zero status so passwordless sudo is unavailable.\n2. Trigger an auto-upgrade via `python manage.py shell -c \"from core.tasks import check_github_updates; check_github_updates()\"`.\n3. After the task finishes, run `systemctl status $(cat locks/service.lck)` and verify the service reports `active (running)`.\n4. Open System → Upgrade Report and confirm the most recent log entry notes the service restart succeeded after the upgrade.",
      "done_on": null,
      "is_deleted": false,
      "version": "0.1.34"
    }
  }
]
