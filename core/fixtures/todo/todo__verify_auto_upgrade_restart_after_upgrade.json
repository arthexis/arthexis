[
  {
    "model": "core.todo",
    "fields": {
      "request": "Manual QA: Confirm auto-upgrade restarts services when an update is applied",
      "created_on": "2025-11-15T00:00:00Z",
      "url": "/admin/system/upgrade-report/",
      "request_details": "1. Prepare a test node that is one release behind origin/main so an upgrade is required.\n2. Start auto-upgrade via `python manage.py shell -c \"from core.tasks import check_github_updates; check_github_updates()\"`.\n3. Monitor logs/auto-upgrade.log and confirm the entry records `./upgrade.sh --latest` (without --no-restart).\n4. After the task finishes, verify `systemctl status $(cat locks/service.lck)` reports the service as active.\n5. Review System → Upgrade Report to ensure the run completed successfully and note whether any manual follow-up is necessary.",
      "done_on": null,
      "is_deleted": false,
      "version": "0.1.35"
    }
  }
]
