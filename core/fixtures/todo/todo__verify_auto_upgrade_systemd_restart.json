[
  {
    "model": "core.todo",
    "fields": {
      "request": "Manual QA: Confirm auto-upgrade relies on systemd restart after signal",
      "created_on": "2025-11-15T00:00:00Z",
      "url": "/admin/system/upgrade-report/",
      "request_details": "1. On a control node installed with systemd management (locks/service.lck present), ensure auto-upgrade is enabled.\n2. Trigger an auto-upgrade run with `python manage.py shell -c \"from core.tasks import check_github_updates; check_github_updates()\"`.\n3. After the task finishes, confirm the managed service PID changed and the unit is active again without manually calling start.sh (systemd should have restarted it after the TERM signal).\n4. If Celery and LCD services are enabled, verify they also restarted and report active.\n5. Review System → Upgrade Report to confirm the restart was captured without manual intervention.",
      "done_on": null,
      "is_deleted": false,
      "version": "0.1.33"
    }
  }
]
