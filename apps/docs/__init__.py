"""Compatibility namespace for helpers retained after removing the docs Django app.

This package is intentionally not a Django application: it has no AppConfig, models,
URLs, admin registration, or runtime documentation routes.
"""
