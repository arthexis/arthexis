from django.apps import AppConfig


class BluetoothConfig(AppConfig):
    """Application configuration for Bluetooth inventory and control."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.bluetooth"
