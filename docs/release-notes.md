# Release Notes


## Upcoming

- **Deprecation/removal:** The legacy `apps.camera` compatibility package has been removed from the distributed package. External integrations should stop importing `apps.camera` and migrate to maintained app modules in the Arthexis suite before upgrading.
