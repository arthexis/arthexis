# Architecture manifest (retired)

This document has been replaced by the dynamic CI node matrix generated from
the `nodes.NodeRole`/`nodes.NodeFeature` fixtures via
`scripts/build_node_ci_plan.py`. Refer to
`docs/development/pytest-role-markers.md` for the current flow.
