# Watchtower deployment composition.
# Keep the public Arthexis application and remote access surface as separate
# child deployments so each can evolve and recover independently.
./arthexis.rx

./remote-expose.rx
